rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Users can access only their data
    match /users/{userId} {
      allow read: if request.auth.uid == userId;
    }
  }
}